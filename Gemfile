source 'https://rubygems.org'

gem 'rails', '3.2.11'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'mysql2' # mySQL DB gem
gem "simple-navigation" # vavigation menus
gem "authlogic" # user authorization/authentication
gem 'acts_as_tenant' # multi tenancy
gem 'kaminari' # pagination
gem 'memoist' # memoization (caching)
gem "prawn", "0.8.4" # PDF generation
gem 'delayed_job_active_record' # background task helper
gem "daemons" # used for delayed_jobs
gem 'fog' #cloud service interface (i.e S3)
gem 'carrierwave_direct' #Upload direct from the user browser
gem 'unf' #works with fog (rails will throw warnings if this gem is not installed)
gem "delayed_job_web" #delayed_job GUI
gem 'activerecord-import', '0.3.1' #mass SQL INSERTs and UPDATEs

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# gem 'rvm-capistrano'

# To use debugger
# gem 'debugger'

gem 'execjs'
